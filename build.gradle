plugins {
	id 'java-gradle-plugin'
	id('com.gradle.plugin-publish') version "1.3.1"
}

group = 'net.apollofops'
version = '2025.0.0'

gradlePlugin {
	website = 'https://github.com/CoffeeCoder1/VendorTools'
	vcsUrl = 'https://github.com/CoffeeCoder1/VendorTools'
	plugins {
		vendorTools {
			id = 'net.apollofops.VendorTools'
			displayName = 'VendorTools'
			implementationClass = 'net.apollofops.vendortools.VendorToolsPlugin'
			description = 'A better build system for WPILib Vendor dependencies.'
			tags = ['frc', 'wpilib', 'gradlerio', 'vendordep']
		}
	}
}

publishing {
    repositories {
        maven {
            name = 'localPluginRepository'
            url = file('../gradle-plugins')
        }
    }
}
